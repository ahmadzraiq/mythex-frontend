/**
 * Legacy icon name map — kept for reference only.
 * The canonical icon component is IconifyIcon (type: "Icon" in JSON config).
 * No Lucide package imports remain; all icons are served via Iconify API.
 */

export {};
