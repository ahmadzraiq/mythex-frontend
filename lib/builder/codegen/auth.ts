/**
 * auth.ts — Auth file emission stub.
 * Auth is now handled via generic variables + appLoad workflow, no special codegen needed.
 */

import type { CodegenCtx, EmittedFile } from './types';

export function emitAuthFiles(_ctx: CodegenCtx): EmittedFile[] {
  return [];
}
