{
  "fetchProduct": {
    "type": "graphql",
    "query": "query GetProductDetail($slug: String!) { product(slug: $slug) { id name description slug assets { id preview source } variants { id name sku priceWithTax stockLevel options { id code name groupId group { id code name } } } optionGroups { id code name options { id code name } } collections { id name slug parent { id } } } }",
    "variables": {
      "slug": "{{route.slug}}"
    },
    "headers": {
      "vendure-token": "__default_channel__"
    },
    "storeIn": "product",
    "responsePath": "data.product",
    "cacheTag": "product",
    "cacheTTL": 300,
    "cacheKeyVars": [
      "route.slug"
    ],
    "endpoint": "http://localhost:3000/shop-api"
  },
  "fetchFeaturedProducts": {
    "type": "graphql",
    "query": "query GetFeaturedProducts($input: SearchInput!) { search(input: $input) { items { productId productName slug productAsset { id preview } priceWithTax { __typename ... on PriceRange { min max } ... on SinglePrice { value } } currencyCode } } }",
    "headers": {
      "vendure-token": "__default_channel__"
    },
    "storeIn": "featured.products",
    "responsePath": "data.search.items",
    "cacheTag": "featured",
    "cacheTTL": 600,
    "endpoint": "http://localhost:3000/shop-api",
    "variables": {
      "input": {
        "collectionSlug": "electronics",
        "take": 8,
        "skip": 0,
        "groupByProduct": true
      }
    }
  },
  "fetchCollection": {
    "type": "graphql",
    "query": "query GetCollectionProducts($slug: String!, $input: SearchInput!) { collection(slug: $slug) { id name slug description featuredAsset { id preview } } search(input: $input) { totalItems items { productId productName slug productAsset { id preview } priceWithTax { __typename ... on PriceRange { min max } ... on SinglePrice { value } } currencyCode } facetValues { count facetValue { id name facet { id name } } } } }",
    "variables": {
      "slug": "{{route.slug}}",
      "input": {
        "collectionSlug": "{{route.slug}}",
        "take": "{{collectionPageSize}} || 12",
        "skip": "{{collectionSkip}} || 0",
        "groupByProduct": true,
        "sort": "{{sortInput}} || {'name': 'ASC'}",
        "facetValueIds": "{{route.facets}} || []"
      }
    },
    "headers": {
      "vendure-token": "__default_channel__"
    },
    "storeIn": "collection",
    "responsePath": "data",
    "cacheTag": "collection",
    "cacheTTL": 120,
    "cacheKeyVars": [
      "route.slug",
      "collectionSkip",
      "sortInput",
      "route.facets"
    ],
    "endpoint": "http://localhost:3000/shop-api"
  },
  "fetchSearchResults": {
    "type": "graphql",
    "query": "query SearchProducts($input: SearchInput!) { search(input: $input) { totalItems items { productId productName slug productAsset { id preview } priceWithTax { __typename ... on PriceRange { min max } ... on SinglePrice { value } } currencyCode } facetValues { count facetValue { id name facet { id name } } } } }",
    "variables": {
      "input": {
        "term": "{{route.q}}",
        "take": "{{collectionPageSize}} || 12",
        "skip": "{{searchSkip}} || 0",
        "groupByProduct": true,
        "sort": "{{sortInput}} || {'name': 'ASC'}",
        "facetValueIds": "{{route.facets}} || []"
      }
    },
    "headers": {
      "vendure-token": "__default_channel__"
    },
    "storeIn": "search",
    "responsePath": "data.search",
    "cacheTag": "search",
    "cacheTTL": 120,
    "cacheKeyVars": [
      "route.q",
      "searchSkip",
      "route.facets"
    ],
    "endpoint": "http://localhost:3000/shop-api"
  },
  "fetchRelatedProducts": {
    "type": "graphql",
    "query": "query GetRelatedProducts($input: SearchInput!) { search(input: $input) { items { productId productName slug productAsset { id preview } priceWithTax { __typename ... on PriceRange { min max } ... on SinglePrice { value } } currencyCode } } }",
    "variables": {
      "input": {
        "collectionSlug": "{{product.primaryCollectionSlug}}",
        "take": 13,
        "skip": 0,
        "groupByProduct": true
      }
    },
    "headers": {
      "vendure-token": "__default_channel__"
    },
    "storeIn": "related._raw",
    "responsePath": "data.search.items",
    "cacheTag": "related",
    "cacheTTL": 300,
    "cacheKeyVars": [
      "product.primaryCollectionSlug",
      "$parent.id"
    ],
    "endpoint": "http://localhost:3000/shop-api"
  },
  "writeReview": {
    "type": "appendToPath",
    "targetPath": "product.reviews",
    "value": {
      "id": "_timestamp",
      "author": "{{form.name}}",
      "rating": "{{form.rating}}",
      "comment": "{{form.review}}",
      "date": "_date"
    },
    "resetFormPath": "screens.product.form",
    "resetFormValue": {
      "name": "",
      "rating": 5,
      "review": ""
    }
  },
  "fetchTopCollections": {
    "type": "graphql",
    "query": "query GetTopCollections { collections(options: { topLevelOnly: true, take: 12 }) { items { id name slug featuredAsset { id preview } } } }",
    "headers": {
      "vendure-token": "__default_channel__"
    },
    "storeIn": "nav.collections",
    "responsePath": "data.collections.items",
    "cacheTag": "collections",
    "cacheTTL": 600,
    "endpoint": "http://localhost:3000/shop-api"
  },
  "fetchNavCollections": {
    "type": "graphql",
    "endpoint": "http://localhost:3000/shop-api",
    "query": "query GetTopCollections { collections(options: { filter: { parentId: { eq: \"1\" } } }) { items { id name slug } } }",
    "headers": {
      "vendure-token": "__default_channel__"
    },
    "storeIn": "nav.collections",
    "responsePath": "data.collections.items"
  },
  "fetchHome": {
    "type": "graphql",
    "endpoint": "http://localhost:3000/shop-api",
    "query": "query GetTopCollections { collections(options: { filter: { parentId: { eq: \"1\" } } }) { items { id name slug } } }",
    "headers": {
      "vendure-token": "__default_channel__"
    },
    "storeIn": "nav.collections",
    "responsePath": "data.collections.items"
  },
  "searchProducts": {
    "type": "graphql",
    "endpoint": "http://localhost:3000/shop-api",
    "query": "query SearchProducts($input: SearchInput!) { search(input: $input) { totalItems items { productId productName slug productAsset { id preview } priceWithTax { __typename ... on PriceRange { min max } ... on SinglePrice { value } } currencyCode } facetValues { count facetValue { id name facet { id name } } } } }",
    "variables": {
      "input": {
        "term": "{{route.q}}",
        "take": 24,
        "skip": 0,
        "groupByProduct": true
      }
    },
    "headers": {
      "vendure-token": "__default_channel__"
    },
    "storeIn": "search",
    "responsePath": "data.search"
  },
  "PersistAPI": {
    "type": "fetch",
    "url": "https://api.example.com/data",
    "method": "GET"
  },
  "toDelete": {
    "type": "fetch",
    "url": "https://delete.me",
    "method": "GET"
  },
  "myAPI": {
    "type": "fetch",
    "url": "https://test.com",
    "method": "GET"
  }
}