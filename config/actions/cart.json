{
  "fetchCart": {
    "type": "graphql",
    "query": "query GetActiveOrder { activeOrder { id code state totalQuantity subTotal subTotalWithTax shipping shippingWithTax total totalWithTax currencyCode couponCodes discounts { description amountWithTax } lines { id productVariant { id name sku product { id name slug featuredAsset { id preview } } } unitPriceWithTax quantity linePriceWithTax } } }",
    "headers": {
      "Authorization": "{{auth.token}}",
      "vendure-token": "__default_channel__"
    },
    "storeIn": "cart",
    "responsePath": "data.activeOrder",
    "skipStoreWhenNull": true,
    "cacheTag": "cart",
    "cacheTTL": 60,
    "cacheKeyVars": [
      "auth.token"
    ],
    "endpoint": "http://localhost:3000/shop-api"
  },
  "addToCart": {
    "type": "graphql",
    "query": "mutation AddToCart($variantId: ID!, $quantity: Int!) { addItemToOrder(productVariantId: $variantId, quantity: $quantity) { __typename ... on Order { id code state totalQuantity subTotal subTotalWithTax shipping shippingWithTax total totalWithTax currencyCode couponCodes discounts { description amountWithTax } lines { id productVariant { id name sku product { id name slug featuredAsset { id preview } } } unitPriceWithTax quantity linePriceWithTax } } ... on ErrorResult { errorCode message } } }",
    "variables": {
      "variantId": "{{product.selectedVariant.id}}",
      "quantity": 1
    },
    "headers": {
      "Authorization": "{{auth.token}}",
      "vendure-token": "__default_channel__"
    },
    "storeIn": "cart",
    "responsePath": "data.addItemToOrder",
    "onSuccess": {
      "action": "showToast",
      "payload": {
        "message": "Added to cart"
      }
    },
    "invalidateCache": [
      "cart"
    ],
    "endpoint": "http://localhost:3000/shop-api"
  },
  "addToCartFromCard": {
    "type": "graphql",
    "query": "mutation AddToCart($variantId: ID!, $quantity: Int!) { addItemToOrder(productVariantId: $variantId, quantity: $quantity) { __typename ... on Order { id code state totalQuantity subTotal subTotalWithTax shipping shippingWithTax total totalWithTax currencyCode couponCodes discounts { description amountWithTax } lines { id productVariant { id name sku product { id name slug featuredAsset { id preview } } } unitPriceWithTax quantity linePriceWithTax } } ... on ErrorResult { errorCode message } } }",
    "variables": {
      "variantId": "{{$item.variantId}}",
      "quantity": 1
    },
    "headers": {
      "Authorization": "{{auth.token}}",
      "vendure-token": "__default_channel__"
    },
    "storeIn": "cart",
    "responsePath": "data.addItemToOrder",
    "invalidateCache": [
      "cart"
    ],
    "endpoint": "http://localhost:3000/shop-api"
  },
  "removeFromCart": {
    "type": "graphql",
    "query": "mutation RemoveFromCart($lineId: ID!) { removeOrderLine(orderLineId: $lineId) { __typename ... on Order { id code state totalQuantity subTotal subTotalWithTax shipping shippingWithTax total totalWithTax currencyCode couponCodes discounts { description amountWithTax } lines { id productVariant { id name sku product { id name slug featuredAsset { id preview } } } unitPriceWithTax quantity linePriceWithTax } } ... on ErrorResult { errorCode message } } }",
    "variables": {
      "lineId": "{{$item.id}}"
    },
    "headers": {
      "Authorization": "{{auth.token}}",
      "vendure-token": "__default_channel__"
    },
    "storeIn": "cart",
    "responsePath": "data.removeOrderLine",
    "invalidateCache": [
      "cart"
    ],
    "endpoint": "http://localhost:3000/shop-api"
  },
  "adjustCartItemQuantity": {
    "type": "graphql",
    "query": "mutation AdjustCartItem($lineId: ID!, $quantity: Int!) { adjustOrderLine(orderLineId: $lineId, quantity: $quantity) { __typename ... on Order { id code state totalQuantity subTotal subTotalWithTax shipping shippingWithTax total totalWithTax currencyCode couponCodes discounts { description amountWithTax } lines { id productVariant { id name sku product { id name slug featuredAsset { id preview } } } unitPriceWithTax quantity linePriceWithTax } } ... on ErrorResult { errorCode message } } }",
    "variables": {
      "lineId": "{{$item.id}}",
      "quantity": "{{$item.quantity}}"
    },
    "headers": {
      "Authorization": "{{auth.token}}",
      "vendure-token": "__default_channel__"
    },
    "storeIn": "cart",
    "responsePath": "data.adjustOrderLine",
    "invalidateCache": [
      "cart"
    ],
    "endpoint": "http://localhost:3000/shop-api"
  },
  "applyPromotionCode": {
    "type": "graphql",
    "query": "mutation ApplyPromotionCode($couponCode: String!) { applyCouponCode(couponCode: $couponCode) { __typename ... on Order { id code totalWithTax couponCodes discounts { description amountWithTax } } ... on ErrorResult { errorCode message } } }",
    "variables": {
      "couponCode": "{{form.couponCode}}"
    },
    "headers": {
      "Authorization": "{{auth.token}}",
      "vendure-token": "__default_channel__"
    },
    "storeIn": "cart._promoResult",
    "responsePath": "data.applyCouponCode",
    "onSuccess": {
      "type": "runMultiple",
      "actions": [
        {
          "action": "fetchCart"
        },
        {
          "type": "set",
          "path": "screens.cart.form.couponCode",
          "value": ""
        }
      ]
    },
    "invalidateCache": [
      "cart"
    ],
    "endpoint": "http://localhost:3000/shop-api"
  },
  "removePromotionCode": {
    "type": "graphql",
    "query": "mutation RemovePromotionCode($couponCode: String!) { removeCouponCode(couponCode: $couponCode) { id code totalWithTax couponCodes discounts { description amountWithTax } } }",
    "variables": {
      "couponCode": "{{$item}}"
    },
    "headers": {
      "Authorization": "{{auth.token}}",
      "vendure-token": "__default_channel__"
    },
    "storeIn": "cart._removePromoResult",
    "responsePath": "data.removeCouponCode",
    "onSuccess": {
      "action": "fetchCart"
    },
    "invalidateCache": [
      "cart"
    ],
    "endpoint": "http://localhost:3000/shop-api"
  },
  "openCartDrawer": {
    "type": "set",
    "path": "layout.cartDrawerOpen",
    "value": true
  },
  "closeCartDrawer": {
    "type": "set",
    "path": "layout.cartDrawerOpen",
    "value": false
  }
}